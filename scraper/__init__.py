"""geosteeze scraper package.

Discovers and validates *live* geographic data endpoints (services that can be
streamed straight into a web map with no download step) and writes them to a
normalized ``catalog.json`` that the Cesium front-end consumes.
"""

__version__ = "0.1.0"
